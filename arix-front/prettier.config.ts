import { type Config } from "prettier";

const config: Config = {
  trailingComma: "none",
  printWidth: 60,
  endOfLine: "auto"
  // plugins: ["prettier-plugin-tailwindcss"]
};

export default config;
